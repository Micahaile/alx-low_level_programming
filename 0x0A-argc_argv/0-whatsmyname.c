#include <stdio.h>
/**
 * main - a program that prints its name
 * @argc: number of arrguement supplied to program
 * @argv: an array of pointer to the program
 * Return: 0 sucess
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
